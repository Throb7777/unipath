# Copy this file to Android/keystore.properties before building a signed release APK.
# Do not commit Android/keystore.properties or your keystore file.

# Path is resolved relative to the Android/ directory.
storeFile=unipath-release.keystore
storePassword=replace-with-your-keystore-password
keyAlias=unipath
keyPassword=replace-with-your-key-password
